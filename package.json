{
  "name": "gimnasio-fidelizacion",
  "private": true,
  "type": "module",
  "scripts": {
    "build:vercel": "npx prisma generate --schema=./apps/api/prisma/schema.prisma && cd apps/web && npm install && npx vite build",
    "postinstall": "npx prisma generate --schema=./apps/api/prisma/schema.prisma || true"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "prisma": "^5.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "@types/node": "^20.11.0"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "zod": "^3.22.4",
    "uuid": "^9.0.1",
    "date-fns": "^3.3.1"
  },
  "packageManager": "npm@10.2.0"
}
